# Local benchmarks use your own TypeSafe key. Never add this to Vercel.
TYPESAFE_API_KEY=
TYPESAFE_MODEL=jev-latest

# Set true for the bundled recording with no live generation.
# Vercel builds require true; changing it requires a rebuild.
REPLAY_ONLY=false
